# Copy to .env for local development. Never commit the real .env (already gitignored).

DATABASE_URL="postgresql://cycove:cycove_dev_only@localhost:5432/cycove"
REDIS_URL="redis://localhost:6379"

PORT=3000
HOST=0.0.0.0

# Firebase Cloud Messaging — opaque wake-signal pushes only, see
# Projects/CyCove.md -> Architecture -> Backend. Not needed until push is implemented.
# FCM_SERVICE_ACCOUNT_JSON=
