---
tags: [virdana, messaging-app, plan]
updated: 2026-07-21
---

# Plan: CyCove

Living execution plan. Check status at the start of every session. See [[.claude/handoff-2026-07-18|latest handoff]] for session context. Full product context lives in [[Projects/CyCove|the CyCove vault note]] (`VirdanaVault\VirdanaVault\Projects\CyCove.md`).

## Phase 0 — Initial Design Process (do this before writing any code)
- [x] App name selected: **CyCove** — see [[Projects/CyCove#Key decisions|Key decisions]] → App name for full reasoning (blank-slate, English-hint, and Latin-hint directions all explored)
  - [x] USPTO TESS trademark search — clear, no results for "CyCove" (2026-07-18)
  - [x] EUIPO TMview trademark search (given EU-first positioning) — clear, only unrelated "CYCOVER"/"BICYCOVER" bicycle-cover marks, different classes (2026-07-18)
  - [x] Domain availability (.com, .app) — both available, confirmed via registry RDAP (2026-07-18)
  - [x] Exact-name search on App Store + Play Store — clear, no exact match on either (2026-07-18)
  - [ ] Formal trademark attorney opinion before filing — not done yet; flag the "Cove Identity App" adjacency (see [[Projects/CyCove#Key decisions|Key decisions]] → App name) specifically
  - [ ] If CyCove fails clearance: fall back to Olvix (blank-slate, also clean in web search) — not currently needed, preliminary search came back clear
- [x] Draft privacy policy & ToS outline (informs data model design, not just legal) — drafted 2026-07-18 as `cycove/PRIVACY_POLICY.md` and `cycove/TERMS_OF_SERVICE.md`; explicitly outlines, needs a lawyer pass before anything ships
- [x] Select EU hosting provider + region — decided 2026-07-18: **Hetzner CAX21, Germany.** See [[Projects/CyCove#Key decisions|Key decisions]] → Hosting
- [x] Formalize API contract as OpenAPI (HTTP) + AsyncAPI or equivalent (WebSocket) spec, based on draft in [[Projects/CyCove#Backend|Architecture → Backend]] — drafted 2026-07-18 at `cycove/docs/api/openapi.yaml` and `asyncapi.yaml`; will drift once client integration starts
- [x] Full threat model write-up (expand the draft in [[Projects/CyCove#Crypto|Architecture → Crypto]]) — written 2026-07-18 at `cycove/THREAT_MODEL.md`; flags safety-number verification and Web storage hardening as the remaining gaps before audit-ready
- [x] Recovery key / account recovery UX design — see [[Projects/CyCove#Key decisions|Key decisions]] → Identity model open question. Drafted 2026-07-18 at `cycove/docs/ux-flows.md`
- [x] Contact-adding flow design (QR code / share-link, since there's no phone-number discovery) — drafted 2026-07-18, same doc (`cycove/docs/ux-flows.md`)
- [x] Wireframes: registration, device linking (QR pairing), chat UI, safety-number verification screen — drafted 2026-07-18 at `cycove/docs/wireframes.md`, layout only, no visual design
- [ ] Legal review: age verification requirement under current EU rules (open question, see [[Projects/CyCove#Open questions|CyCove → Open questions]]) — researched 2026-07-18 (GDPR Art. 8, EU DSA age-verification blueprint), not yet reviewed by an actual lawyer
- [x] Repo scaffolding: monorepo vs. separate repos for backend/web/android — decided and set up 2026-07-18. Monorepo (npm workspaces) for `backend` + `web` since they share TypeScript types; Android stays a separate repo (Kotlin, nothing to share). `cycove/package.json`, `backend/`, `web/` workspace shells (package.json + tsconfig only, no app code), git initialized, no commits made
- [x] CI/CD pipeline skeleton — `.github/workflows/ci.yml` set up 2026-07-18 (typecheck + lint + backend build via GitHub Actions), verified passing locally. `build-web` and `deploy` jobs deliberately omitted until a real Next.js app scaffold and a provisioned VPS + Dockerfile exist, respectively
- [x] Provision the Hetzner box — done 2026-07-21, well ahead of the original ~August budget target (Ben opted to pay Hetzner's one-time 25€ identity verification early). **CX23** (2 vCPU/4GB/40GB, Nuremberg), not the originally planned CAX21 — Arm64 CAX line was out of stock EU-wide (checked Nuremberg, Falkenstein, and Helsinki, all unavailable), x86 CX23 accepted as a fine fallback since it removes rather than adds a concern (no Arm64 Docker image compatibility question) and is sufficient for Phase 1 scale. Debian 13 (trixie), public IPv4+IPv6, no private networking. SSH hardened (non-root sudo user, `PermitRootLogin no`, `PasswordAuthentication no`), Hetzner Cloud Firewall applied (TCP 22 restricted to Ben's static IP `/32`; 80/443 open to any once the reverse proxy shipped — see below), Docker CE + Compose plugin installed via the official trixie-native apt repo.
- [x] Deploy backend to production — done 2026-07-21. See changelog entry below for the full sequence (git history creation, GitHub remote, deploy key, Caddy reverse proxy + TLS). Live at `https://cycove.virdana.dev`.

## Phase 1 — MVP Build (1:1 messaging, Android + Web, multi-device)
- [x] Backend scaffolding (2026-07-18): Fastify app shell (health check + WS route stub), Prisma schema matching the data model, Dockerfile, local dev `docker-compose.yml` (Postgres + Redis). Verified booting.
- [x] Backend: account registration (2026-07-19) — `POST /register`, creates User + first Device, issues a bearer session token. Device *linking* (`/devices/link`) still open — see below.
- [x] Backend: prekey bundle storage/distribution (2026-07-19) — `POST /prekeys` (upload), `GET /prekeys/:userId` (atomic claim-and-consume, race-tested logically). Both authenticated via the new opaque bearer-token scheme (`backend/src/lib/auth.ts`).
- [x] Backend: `POST /devices/pairing-token` + `POST /devices/link` (2026-07-19) — added the pairing-token issuance endpoint (missing from the original spec) then the linking endpoint that consumes it. Single-use, 5-minute TTL.
- [x] Backend: WebSocket relay + offline queue (2026-07-19) — `GET /v1/connect`: auth-as-first-message (verified with a real WS client — unauthenticated first message correctly rejected with 4001 close), `sendCiphertext`/`deliveryAck`, `queueFlush` on connect, in-memory connection registry. Push trigger (FCM) still not implemented — needs Firebase credentials, tracked as a TODO in the code.
- [x] Backend: `POST /messages` (2026-07-19) — HTTP fallback send, shares `relayMessage()` with the WS path so both transports behave identically.
- [x] Live end-to-end verification against a real Postgres/Redis (2026-07-19) — first-ever migration applied, then a full real test pass: both `/register` calls, prekey upload + atomic claim-and-consume (confirmed the race-safety logic works — second fetch correctly 404s), pairing-token issuance + device linking + confirmed single-use, and the full WS relay (auth gate, empty queueFlush on fresh connect, live send→deliver between two real WS clients, deliveryAck confirmed via direct DB check to delete the row, and the offline path — HTTP fallback send while recipient was disconnected, delivered via queueFlush on reconnect). Test data cleared after. Phase 1 backend is now genuinely done, not just typechecked.
- [ ] Android: matrix-sdk-crypto integration (UniFFI bindings)
- [ ] Android: chat UI (Compose)
- [ ] Android: device linking (QR pairing, as a new device)
- [x] Web: matrix-sdk-crypto-wasm integration (2026-07-19) — real Olm encryption verified working live: two browser tabs, two real accounts, messages exchanged both directions, wire ciphertext decoded and confirmed genuine (never plaintext). Required reshaping the backend's key endpoints — see Projects/CyCove.md → Key decisions → Crypto library and `docs/crypto-integration-notes.md`.
- [x] Web: chat UI (Next.js) (2026-07-19) — real multi-contact product UI replacing the bare test harness: sidebar + conversation view, share-code contact adding with optional nicknames, sent/delivered message bubbles (real delivery receipts, not just a visual guess), per-contact SAS verification, everything persisted across reloads. See changelog below and `docs/wireframes.md` → screen 03.
- [x] Web: mandatory verification, contact requests, usernames, presence signals, avatars (2026-07-19) — verification is now a hard gate (composer replaced by a call-to-action until a contact is verified, both add-contact paths); a second "send a request" flow sits alongside direct-connect, requiring explicit accept before the two accounts are mutually connected (**direct-connect later removed — see changelog below, it had a one-sided synchronization gap that broke verification**); self-chosen usernames embed into share codes as a local label (**superseded same day — see the line below, this became a real server-side directory**); live typing indicators and sent/delivered/read receipts (both ephemeral or opaque to-device events, no backend changes); per-contact local-only avatars, editable after the fact. Live-tested with three real accounts including a fresh, never-verified pair to actually exercise the new gate. Found and fixed one real bug along the way (decline handler missing an `ensureSessionWith` call — later found to be unnecessary rather than wrong, see the line below). See changelog below, `docs/crypto-integration-notes.md`, `docs/ux-flows.md`, and `THREAT_MODEL.md` → MITM at first contact.
- [x] Rate limiting, real username directory, encrypted contact backup (2026-07-19) — usernames became a genuine server-resolvable directory (`PUT /users/username`, `GET /users/by-username/:username`), reversing the local-only-label decision from earlier the same day once it became clear that's what "type a username to add someone" actually requires; a hand-rolled Redis fixed-window rate limiter (`backend/src/lib/rateLimit.ts`) protects registration, username claim/lookup, and contact-request sending; contacts now back up to the server as a client-encrypted opaque blob (`PUT`/`GET /account-data/contacts`, recovery-key-derived AES-GCM, WebCrypto — explicitly *not* built on matrix-sdk-crypto-wasm's key-backup feature, which turned out to be Megolm-room-key-specific, not generic — see changelog below). Sending a contact request moved off the encrypted to-device relay onto its own rate-limitable HTTP endpoint, since the server can't target opaque Olm ciphertext for rate limiting — accept/decline stay on the relay but as plain (non-encrypted) content. See changelog below, `docs/crypto-integration-notes.md`, `THREAT_MODEL.md`, and `docs/api/openapi.yaml`.
- [x] Web: device linking (2026-07-20) — pairing-code mechanism (copy-paste at first, **QR generation + camera scanning added later the same day**, see below) fully built and live-tested: `requestPairingCode()`/`CyCoveCrypto.linkDevice()`, new `LinkDevicePanel.tsx`. Same-device login (`POST /login`) also shipped in this pass — see changelog below.
- [x] Web: QR-code device linking + linked-devices list with revoke (2026-07-20, later same day) — `qrcode`/`jsqr` for generation/scanning (new `QrScanner.tsx`), `GET /devices` + `DELETE /devices/:deviceId` (new `DevicesPanel.tsx`) as the safety net QR's real-world threat model change calls for. See changelog below and `THREAT_MODEL.md` → Unauthorized device linking via QR capture.
- [ ] Cross-signing + encrypted key backup implementation — still unbuilt; the new *contact* backup above is a different, simpler mechanism (plain WebCrypto, not this feature) and doesn't complete this item
- [x] Safety number / device verification UI (2026-07-19) — real contact-adding via share code + full SAS emoji verification flow, live-tested with two real accounts. Made mandatory before messaging the same day — see the line above.
- [ ] Freemium gating: single-device free tier vs. multi-device paid tier
- [ ] Third-party security audit (before any public launch)

## Phase 2 — Groups + Monetization Expansion
- [ ] Megolm group messaging implementation
- [ ] Group admin controls
- [ ] Business/team tier (SSO, org policies)

## Phase 3 — iOS
- [ ] Swift client, matrix-sdk-crypto via UniFFI Swift bindings

## Ongoing / Watch Items
- [ ] Monitor EU CSA Regulation ("Chat Control 2.0") trilogue outcome — next round September 2026. No action required unless a detection-order mandate is adopted; see [[Projects/CyCove#Open questions|CyCove → Open questions]].

## Changelog
- 2026-07-18: Plan created following initial architecture session.
- 2026-07-18: Vault notes consolidated into a single `Projects/CyCove.md` hub note (matching the vault's one-note-per-product convention, per Clurio/Pulse) rather than kept as separate Architecture/Decision files; links above updated accordingly. Placed in both the VirdanaVault vault and this repo's `.claude/` folder, per Ben's decision in the Claude Code session.
- 2026-07-18: Preliminary name-clearance search completed for CyCove — USPTO TESS, EUIPO TMview, `.com`/`.app` domain availability, and App Store/Play Store exact-name checks all came back clear. Formal trademark attorney opinion still outstanding before filing.
- 2026-07-18: Continued through the rest of Phase 0's writing/research items — hosting provider recommendation, privacy policy & ToS outlines, threat model, API contract (OpenAPI + AsyncAPI), recovery/contact-adding UX design, wireframes, and EU age-verification research. Deliberately stopped short of repo scaffolding, CI/CD, and VPS provisioning — those create persistent infrastructure or cost money and need an explicit go-ahead first.
- 2026-07-18: Given the go-ahead on repo scaffolding — set up the `cycove` monorepo (npm workspaces, `backend`/`web` shells, git initialized) and researched concrete VPS options with current pricing (Hetzner's June 2026 price adjustment significantly changed the picture — CAX21 is now the best value pick, not the CPX line originally assumed). CI/CD and VPS provisioning still outstanding.
- 2026-07-18: Set up the CI/CD pipeline skeleton and verified it passes locally. Checked Ben's OVHcloud VPS-2 find against the provider's site — specs confirmed accurate, genuinely competitive with the Hetzner recommendation. Discussed self-hosting on Ben's Synology NAS instead: fine and worth doing for local-only Phase 1 development, not recommended for anything publicly reachable (residential uptime/bandwidth/CGNAT/ISP-ToS concerns, plus NAS-specific ransomware exposure). VPS provisioning is now the only remaining Phase 0 infrastructure item.
- 2026-07-18: Hosting provider decided — Hetzner CAX21, Germany. Provisioning deferred ~1 month for budget; not a blocker since nothing in Phase 0 / early Phase 1 needs public reachability. Plan: build against the NAS locally now, migrate to Hetzner when opening up to real users / needing real-mobile-network testing. Migration is low-effort given the dumb-pipe architecture has no server-side state to move.
- 2026-07-18: Checked the actual NAS model (DS223j, DSM 7.3.2-86009 Update 4) before scaffolding on it — it's ARM + 1GB RAM and can't run Container Manager/Docker (x86-only on DSM). Revised the sequencing plan: local dev via Docker Desktop on Ben's Windows machine instead. Started Phase 1 backend scaffolding (Fastify shell, Prisma schema, Dockerfile, docker-compose.yml) — verified working end-to-end (schema validates/generates, typecheck passes, server boots, `/health` responds). Route business logic (register, devices/link, prekeys, messages, WS relay) is the next slice of work.
- 2026-07-19: Correction — Ben installed Container Manager on the DS223j directly and it works, contradicting the 2026-07-18 research. NAS is the primary dev target again; Windows Docker Desktop stays documented as a fallback (not installed). 1GB RAM is a real thing to watch, not a reason to avoid trying it.
- 2026-07-19: Implemented `POST /register`, `POST /prekeys`, `GET /prekeys/:userId` plus the shared Prisma/Redis/auth lib modules. Typecheck caught two real bugs before they shipped (Prisma `Bytes` is `Uint8Array` not `Buffer`; `ioredis` needs its named export under this project's module resolution) — both fixed and re-verified. Updated `docs/api/openapi.yaml` to match the actual implementation (session_token in the register response, simplified prekey-upload shape). No live DB test yet — no Docker available in the environment doing this work.
- 2026-07-19: Renamed the git default branch from `master` to `main` (Ben's naming convention, no commits existed yet so a clean rename). Added `@fastify/cors` (wide open for now — unexposed LAN-only dev, revisit before any public exposure) since the web client and backend run on different ports and the browser would otherwise block the fetch calls. Fixed a port collision — Next.js and the backend both default to 3000; `web`'s dev script now pins `-p 3001`.
- 2026-07-19: Finished the rest of the Phase 1 backend HTTP + WS surface — device pairing/linking, the WebSocket relay (auth-as-first-message, send/ack/queue-flush), and the HTTP message fallback. Added `senderDeviceId` to the `MessageQueue` schema (needed for recipients to know who sent a message — the original schema didn't have it). Verified with typecheck, build, a real server boot, and an actual `ws` client connection exercising the auth-rejection path. `docs/api/openapi.yaml` and `asyncapi.yaml` updated to match, including a `type` discriminator field the AsyncAPI draft implied but never specified. Live Postgres/Redis round-trip still not exercised — needs Docker, which isn't available where this work is happening.
- 2026-07-19: Ben hit `address already in use (0.0.0.0:5432)` trying to start Postgres/Redis on the NAS, and separately asked about a DSM Web Service Portal — which turned out not to apply anyway (it's an HTTP reverse proxy; Postgres/Redis are raw TCP). Reconsidered the split-machine setup: since the backend already runs on Windows, there was no remaining reason for Postgres/Redis to live on the NAS. Decided to run the whole local stack (Postgres, Redis, backend) on Windows via Docker Desktop instead — simpler, sidesteps the NAS's RAM/DSM friction entirely. Docker Desktop needs installing (manual step, not done yet). NAS dropped from the dev loop; kept in mind only as a possible future bridge for real-mobile-network testing before Hetzner is up.
- 2026-07-19: Docker Desktop was installed but couldn't start ("Docker Desktop is unable to start") — root cause was WSL2 not being installed on the machine at all. Ben ran `wsl --install` as Administrator and restarted; Docker came up clean afterward with no port conflict. Ran the first-ever live migration and a full real end-to-end test pass across every Phase 1 backend endpoint and the WS relay (see Phase 1 checklist above) — all passed. Phase 1 backend is now verified working, not just typechecked/built. Test data cleared from the database afterward.
- 2026-07-19: Given the go-ahead to build the web client and crypto together. Grounded the integration in real `OlmMachine` behavior (throwaway Node script — see `docs/crypto-integration-notes.md`) before writing backend code, which surfaced a real architecture mismatch: `matrix-sdk-crypto-wasm` speaks Matrix's actual Client-Server API shapes, not a generic encrypt/decrypt interface. Asked Ben how to handle it; decided to adapt the backend's key endpoints (`/keys/upload`, `/keys/query`, `/keys/claim` replacing `/prekeys`) rather than switch to a lower-level library, to keep cross-signing/verification/backup available later. Also found mid-build that `user_id`/`device_id` need to be client-generated (the signed key blob bakes them in before `/register` is ever called) and that the WS relay needed a `sender_user_id` field — both fixed. Built `web/src/lib/crypto.ts` (OlmMachine wrapper) and a minimal Next.js test UI. Hit an SSR-vs-WASM bundling failure in `next build`, fixed with `next/dynamic` + `ssr:false`.
- 2026-07-19: Docker Desktop crashed again on relaunch (stuck `dockerInference` socket file from a previous session, `wsl --shutdown` didn't clear it — needed a full machine restart). Once up: renamed the git default branch to `main` (Ben's convention, no commits existed yet), added `@fastify/cors` (wide open — LAN-only dev, not public) and fixed a port collision (backend and Next.js both default to 3000; `web` now pins `-p 3001`). Ben ran the full live test himself: two browser tabs, two real accounts, messages exchanged both directions, wire ciphertext decoded and confirmed genuine Olm ciphertext, never plaintext. Real E2EE confirmed working end-to-end.
- 2026-07-19: Discussed what's actually proven (message content, yes) vs. not (metadata, MITM/safety-number verification, independent audit, local storage hardening — all pre-existing documented gaps, not new). Covered EU regulatory questions — no one can guarantee against future legislation, but current posture (EU hosting, DSA age-verification watch, Chat Control monitoring) is deliberate, not accidental. Asked Ben: no lawyer yet, incorporation jurisdiction is Germany (matches hosting), holding off on deeper Chat Control/GDPR research until closer to real users. Wrote `cycove/LEGAL_BRIEF.md` to make the first meeting with counsel efficient once found — not legal advice, just organizing the open questions in one place.
- 2026-07-19: Ben asked what to build next; recommended closing the MITM/TOFU gap flagged in `THREAT_MODEL.md` with real safety-number verification, since the crypto library (`matrix-sdk-crypto-wasm`) already supports it and the groundwork (grounded SAS protocol sequence, relay redesign to a generic to-device transport) was already done in the prior session. Ben delegated fully ("do your recommendation, I trust the process"). Rewrote `web/app/ClientApp.tsx`: fixed four stale API calls left over from the crypto/ws library rewrite, added a share-code contact-adding flow (base64-encoded `{user_id, device_id}`, replacing raw two-field ID pasting), and a full SAS verification UI (request/accept, 7-emoji comparison, confirm/reject, verified badge). First live two-tab test run stalled — the confirming side's emoji matched but `isDone()` never flipped. Debugged with temporary console logging rather than guessing: found two real bugs, not one. (1) A React stale-closure bug in `ClientApp.tsx` — the WS message handler was captured once by a `useEffect` at mount, so its `verifPhase === 'incoming-pending'` check always read a stale value and the acceptor never auto-accepted the SAS method; fixed with a ref instead of state. (2) A genuine backend race in `backend/src/routes/connect.ts` — the WS `message` handler was `async` but `ws` doesn't await listeners, so two `sendToDevice` calls sent back-to-back (a SAS `mac` immediately followed by its `done`) could have their DB writes resolve out of order, delivering `done` before `mac` and silently stalling verification; fixed by serializing message handling per connection with a promise queue. Re-ran the full flow repeatedly after both fixes — reliably reaches `✓ Verified` on both sides every time, and normal messaging still works post-verification. Full writeup in `docs/crypto-integration-notes.md`; `THREAT_MODEL.md`'s MITM-at-first-contact line updated from "not yet built" to built-but-optional.
- 2026-07-19: Asked Ben what to build next again; recommended the real web chat UI (wireframe screen 03) since crypto + verification were both now proven and the bare test harness wasn't something usable day to day. Ben agreed. Planned via EnterPlanMode before building (multi-file, real architecture decisions — persistence, per-contact verification state, component split); asked one clarifying question first (build real delivery receipts vs. skip for now) — Ben chose real receipts. Built: `web/src/lib/store.ts` (Contact/ChatMessage types, sessionStorage persistence — deliberately sessionStorage not localStorage, same per-tab-isolation reasoning as the existing session key, plus share-code encode/decode moved here from ClientApp.tsx), a `crypto.ts` extension (`sendMessage` now generates/embeds a client-side message id and returns it; `handleIncoming` returns a discriminated `{kind: 'message'|'receipt'|'none'}` result and auto-sends an encrypted `m.cycove.receipt` to-device event back to the sender on successful message decrypt — same generic to-device wire mechanism as everything else, no backend changes needed), four new presentational components under `web/app/components/` (Sidebar, AddContactPanel, Conversation, VerificationPanel), and a full rewrite of `ClientApp.tsx` as the multi-contact orchestrator (verification state — request/SAS refs, phase, emoji — moved from single global refs to per-contact-userId maps; a new hydration effect re-derives verified status for restored contacts via `session.isPeerDeviceVerified()`, the crypto library's own persistent IndexedDB record, rather than trying to persist that ourselves). Typecheck clean on the first pass. Live two-tab test: added each other as contacts with nicknames, confirmed both immediately showed `✓ Verified` (carried over correctly from the prior session's verification via the hydration effect), sent messages both directions and watched bubbles flip from "✓ sent" to "✓✓ delivered" on real receipt round-trips, then reloaded both tabs mid-conversation and confirmed contacts/nicknames/message history/verified badges all survived intact. `docs/wireframes.md` screen 03 marked built.
- 2026-07-19: Ben asked for a large bundle at once — mandatory verification before messaging, self-chosen usernames, a request/accept contact flow, typing indicators, read receipts, and profile pictures — and explicitly invited both clarifying questions and additional ideas. Suggested (not built, flagged as future follow-ups): blocking/reporting, presence/online status, muting, rate-limiting on requests. Asked three real architecture questions via AskUserQuestion rather than guessing on any of them, since one cut against an already-made design decision: (1) what "both methods" meant — resolved as two parallel add-contact flows (direct-connect stays, request/accept is new and separate), not two alternate verification policies; (2) whether usernames should be a searchable server directory or a local label only — Ben chose local-only, preserving the original no-phone-number/no-discoverability identity model rather than reversing it; (3) whether profile pictures sync to contacts or stay local-only like the nickname — Ben chose local-only. Planned via EnterPlanMode given the size (7 files touched, several new event types) before writing any code. Built: `store.ts` extended (`Contact.status`/`theirUsername`/`avatarDataUrl`, `ChatMessage.status` gains `'read'`, own-username persistence, share-code v2 embedding a username, `fileToAvatarDataUrl` canvas-resize helper); `crypto.ts` extended (`encryptAndSend` private helper extracted from the duplicated get-device-and-send pattern, five new send methods, `IncomingResult` grows to 8 variants); two new components (`Avatar.tsx`, `EditContactPanel.tsx`) and three extended (`Sidebar.tsx` pending-request rows, `AddContactPanel.tsx` two-mode tabs + own-username field, `Conversation.tsx` verification-gated composer + typing line + three-state read ticks); `ClientApp.tsx` grew to route five new event kinds and own the new contact-status/typing/read-receipt state. Typecheck clean on the first pass. Live three-account test (a fresh never-verified pair specifically to exercise the new mandatory gate, since the two long-running test accounts were already mutually verified from every prior session) found and fixed one real bug: `handleDeclineRequest` was missing the `ensureSessionWith` call `handleAcceptRequest` already had, so declining threw "No known device" and silently did nothing — confirmed by tracing with temporary console logging (a coordinate-based click tool also turned out to be unreliable mid-session; switched to dispatching clicks via JS directly, which reliably surfaced the real error). Full pass after the fix: request sent → accepted → composer correctly blocked pending verification → verified → composer unlocked; decline removes the pending entry on both sides; typing indicator appears and auto-clears; a message sent into an already-open conversation goes straight to "read"; username pre-filled the nickname suggestion on the receiving side; avatar upload (synthetic canvas image, since no real file picker in this environment) rendered immediately and survived a reload. Also fixed a minor UX rough edge found live — the "Verify this contact" call-to-action took two clicks (open panel, then actually start) instead of one. `THREAT_MODEL.md`, `docs/wireframes.md`, and `docs/ux-flows.md` all updated to reflect the mandatory gate and what was actually built vs. what the original wireframes assumed (QR/link mechanics still not built — no camera, no domain).
- 2026-07-19, later the same day: Ben asked for three more things — rate limiting on contact requests, server-side contact persistence without message persistence ("is that an issue as well?"), and a reversal of that morning's username decision (type a username to add someone, not just a local label — share codes are too long to hand-copy in practice). Gave a real recommendation rather than just building what was literally described: encrypted backup blob, not a plaintext contacts table, since a plaintext table would be the server's first durable queryable social graph, a materially bigger metadata commitment than anything the design makes today. Asked three clarifying questions before planning: encrypted-vs-plaintext backup (Ben chose encrypted), whether usernames need to be unique/claimed for lookup to work at all (yes, needed), rate-limit scope (broad — username lookup is a spam target too, not just requests). Planned via EnterPlanMode. Two real corrections surfaced *during* research/implementation, before or instead of writing wrong code: (1) planned to build contact backup on `matrix-sdk-crypto-wasm`'s existing key-backup feature — checking the actual type definitions first showed that's Megolm-room-key-specific, not a generic encrypted-blob facility, so built it on plain WebCrypto (PBKDF2 + AES-GCM) instead, and left the real Phase 1 key-backup item exactly as unbuilt as before; (2) the plan's rate-limiting design assumed the server could inspect `event_type` on WS contact-request messages, which is impossible — they're Olm-encrypted by the time they reach the server, indistinguishable from any other message. Moved contact-*request* sending to a new plain HTTP endpoint (`POST /contacts/request`) that can carry a normal per-route rate limit; accept/decline stay on the to-device relay but as plain (non-Olm-encrypted) content via a new `sendPlain` method, for the same reason verification messages already are. This also made the earlier decline-handler `ensureSessionWith` fix unnecessary rather than wrong (no Olm session needed for a plain send) — left in place, harmless, not worth removing for its own sake. Built: `backend/src/lib/rateLimit.ts` (Redis fixed-window, hand-rolled to match the project's existing small-utility convention rather than adding `@fastify/rate-limit`); `User.username` (unique, lowercase, migration applied via a manually-written `migration.sql` + `prisma migrate deploy`, since `prisma migrate dev` refuses to run non-interactively) and new `EncryptedBackup` model; `backend/src/routes/{users,contacts,accountData}.ts`; `crypto.ts` gained backup key derivation/persistence, encrypt-upload/fetch-decrypt, username claim/lookup, and `register()` now takes the raw recovery key (needed for backup-key derivation, not just the hash); `AddContactPanel.tsx` redesigned so both add-contact modes accept a username lookup or a share code interchangeably, resolved to the same shape before either flow runs; a real "Set" action for the caller's own username, not just a local field. Blocked partway through by a recurring Prisma client-generation file lock — initially misattributed to Ben's own terminal and asked him to stop it, which he correctly pointed out he couldn't since he had no terminals open; the actual cause was an orphaned background `tsx watch` dev-server process this session itself had started earlier and forgotten about, respawning under a new PID each time a backend file changed. Killed it directly once correctly identified. `THREAT_MODEL.md` gained two new disclosures (server now durably stores a real, if encrypted, thing for the first time; contact-request/accept/decline are plaintext at the relay layer by necessity) and closed the long-open rate-limiting backlog item. `docs/api/openapi.yaml`/`asyncapi.yaml` updated with the four new endpoints and the plain-vs-encrypted event_type distinction.
- 2026-07-19, continued: Live-tested the Phase 3 bundle end-to-end. Username claim (including case-insensitive collision rejection), add-by-username on both "Connect directly" and "Send a request" modes, and the contact-request rate limiter (429 confirmed at request #20, with request #1–19 in that specific test returning 500s from a pre-existing, unrelated gap — `relayToDevice`'s `to_device_queue` insert has no defensive handling for a nonexistent `recipient_device_id`, shared by `POST /messages`, not something a real client triggers since real recipient IDs always come from an actual lookup — noted, not fixed, out of scope) all passed cleanly. Decline did not: the recipient's own contact list updated fine, but the original requester sometimes never found out, staying stuck on "Request sent — waiting…" forever. Traced with temporary logging in `sendPlain` (`crypto.ts`) — confirmed the send call was reached with a bound sender — then found the real cause by reading `web/src/lib/ws.ts`: `RelayConnection` opened one `WebSocket` at construction and never reconnected on close/error, and `sendToDevice`/`ack` called `.send()` with no `readyState` check. A WebSocket silently no-ops `.send()` in `CLOSING`/`CLOSED` state rather than throwing, so a decline sent during the reconnect window after a connection blip (a real `WebSocket error` was visible in the console immediately before both failed attempts) vanished with no signal anywhere. Fixed by giving `RelayConnection` its own internal reconnect loop (capped exponential backoff, same public interface so `ClientApp.tsx`'s existing `attachSender` binding survives reconnects transparently) and an outbox that queues frames sent while not `OPEN`, flushed on the next successful auth. Confirmed fixed with a clean repro (fresh account, fresh request, all three tabs reloaded first to guarantee the new code was actually running) — decline delivered and the sender's UI updated correctly, repeatably. Also confirmed the backup-restore sub-test: cleared just the `cycove_contacts` sessionStorage key (not the whole session — full from-scratch account recovery via recovery key is a separate, still-unbuilt flow, not what this pass's backup mechanism does) on an account with one connected contact, reloaded, and the contact reappeared correctly from the server's encrypted backup. Full writeup in `docs/crypto-integration-notes.md`. Phase 3 bundle is now fully live-tested, not just typechecked.
- 2026-07-19, continued: Ben tried the app from a Tailscale IP and hit `crypto.randomUUID is not a function`. Root cause is a browser platform restriction, not an app bug: `crypto.randomUUID()` and `crypto.subtle` (the WebCrypto API this app's AES-GCM/PBKDF2 backup encryption and `matrix-sdk-crypto-wasm` both depend on) are only exposed in a *secure context* — HTTPS or `localhost` — and a plain `http://` Tailscale IP doesn't qualify. A one-line polyfill for just `randomUUID` would have failed one line later at `crypto.subtle`, so flagged the real scope before touching anything. Asked Ben how to get HTTPS in place; he picked Tailscale Serve (real trusted certs via the tailnet, no code changes needed) over a self-signed Next.js dev cert. That needed two account-level settings neither of us could flip by API — HTTPS Certificates and MagicDNS, both in the Tailscale admin console DNS settings, MagicDNS being a dependency of HTTPS certs that only surfaced once the first attempt still 500'd after enabling HTTPS Certificates alone. Ben enabled both. Set up `tailscale serve --https=443 http://127.0.0.1:3001` (web) and `--https=8443 http://127.0.0.1:3000` (backend), pointed `web/.env.local`'s `NEXT_PUBLIC_API_BASE` at the backend's new tailnet HTTPS URL (already gitignored), and restarted both dev servers to pick up the change. First real test hit a second, different error — `POST /register` 429'd — which turned out to be the *existing* 5/hour-by-IP register rate limiter correctly firing, not a new bug: `tailscale serve` proxies to the backend over `127.0.0.1`, and Fastify's `request.ip` defaults to the raw socket address, so every proxied request (from any real tailnet device) was landing in the same rate-limit bucket as all of this session's earlier localhost testing. Fixed properly rather than just clearing the counter and moving on: added `trustProxy: 'loopback'` to the Fastify constructor (`backend/src/index.ts`) — scoped to loopback specifically, not blanket trust, since the backend's `HOST=0.0.0.0` also makes port 3000 directly reachable on the tailnet, bypassing the HTTPS proxy entirely; a naive `trustProxy: true` would let a direct caller spoof `X-Forwarded-For` and defeat the rate limiter on purpose. Verified with a direct curl through the Tailscale URL — the rate-limit key correctly showed the real tailnet IP, not `127.0.0.1`. Confirmed the full stack working end-to-end over `https://desktop-invmjie.tail8ce1b.ts.net`: registration, WebCrypto-backed key generation, and the WS relay (`wss://` through the same proxy) all working.
- 2026-07-19, continued: Ben reported trying the app for real over Tailscale and hitting a broken verification flow — "the emoji didn't match" with no emoji selector ever appearing on one side, while the request-based flow's verification worked fine. Also asked (in priority order): fix verification (most important), a way to log back into an existing account/username after closing the tab (currently sessionStorage-only, lost on tab close), and "direct connection should be fixed or made easier somehow" — plus a bunch of UI/UX polish explicitly deferred to later. Diagnosed by reading `handleConnect` (`ClientApp.tsx`): "Connect directly" only ever added the peer to *your own* contact list — no notification, no reciprocal action on the other side, unlike "Send a request" where `contact_accept` is a real synchronizing network event that updates both sides together. If the other person never independently also clicked "Connect directly" on you, they had no `Contact` entry to route an incoming SAS verification request to at all — the request went out fine, but nothing on their screen could ever surface it. The generic `VerificationPanel.tsx` copy for the `cancelled` phase ("the emoji didn't match, or it was rejected") doesn't distinguish a real mismatch from this — a request that was never receivable in the first place looks identical to a real rejection from the UI alone, which is exactly what made this confusing to diagnose from the user-facing symptom. Confirmed the mechanism live before touching code: registered two fresh accounts, connected only one side via direct-connect, started verification — reproduced a hang with no selector on the non-connecting side. Ben interrupted mid-repro with a decisive call: remove "Connect directly" entirely rather than fix its synchronization gap, since the request flow already demonstrably works and doing both is redundant. Removed `handleConnect` from `ClientApp.tsx` and the mode-tab UI from `AddContactPanel.tsx` — request/accept (with either username or share-code resolution, unchanged) is now the only add-contact path. Re-tested clean with two more fresh accounts end to end: request → accept → verify → identical emoji on both screens → both sides confirm → both reach `✓ Verified — safety numbers match`. `docs/ux-flows.md` and `THREAT_MODEL.md` updated to describe only the one remaining flow and document why the other was removed, not just that it was. Cross-device login (recovery-key-based re-authentication) is a separate, larger, not-yet-scoped feature — deliberately not bundled into this same pass given "most important now is the verification"; next up once this is confirmed solid.
- 2026-07-19/20, continued: Ben asked to plan the cross-device login flow. Before designing anything, read `web/src/lib/crypto.ts`'s `register()`/`restore()` closely and found a grounding fact that reshaped the whole plan: `OlmMachine.initialize()` persists to IndexedDB, not sessionStorage — durable, survives tab close. So "access from any device" isn't one problem, it's two: (1) same browser, lost session — the keys are already sitting there safely, just need a way to reconnect — and (2) a genuinely different device, which needs either new linked-device keys or real encrypted key-backup restore (the latter a much bigger, separate lift). Also found the backend already had exactly the right server-side pieces for device linking (`POST /devices/pairing-token`, `POST /devices/link`) built back in Phase 1 and simply never wired into the web client. Asked Ben to scope via AskUserQuestion — same-device login only, device linking only, or both — he chose both in one pass. Planned via `EnterPlanMode`, flagging a real footgun found by reasoning through the design carefully: naively reattaching to an account on a browser that doesn't actually have its keys would silently generate a *new*, mismatched local identity rather than erroring — caught before writing any code by noting that `register()`'s fresh/empty-store path always produces a `KeysUploadRequest` right after `initialize()`, while `restore()`'s already-populated-store path never does; built the login flow to check for this and refuse rather than corrupt the session. Ben added one constraint before approving: keep the plain test-harness UI exactly as-is, no visual redesign — saved as a standing preference, not just followed for this pass.

  Built: `backend/src/routes/login.ts` (`POST /login`, mirrors `/register`'s auth pattern — username + `recovery_key_hash`, rate-limited, reissues a session token for the existing device); `web/src/lib/store.ts` gained `encodePairingCode`/`decodePairingCode` (mirrors the existing share-code pattern exactly); `web/src/lib/crypto.ts` gained `clearSession()`, `CyCoveCrypto.login()` (with the mismatch check above), `CyCoveCrypto.linkDevice()` (generates fresh keys under an *existing* `userId` from a decoded pairing code, otherwise identical to `register()`'s key-upload flow), and `requestPairingCode()`; `ClientApp.tsx` gained Register/Log in/Link-device tabs on the pre-auth screen, `handleLogin`/`handleLinkDevice`/`handleRequestPairingCode`/`handleLogout`; new `LinkDevicePanel.tsx` (pairing-code display, live countdown, regenerate), matching the existing `AddContactPanel`/`EditContactPanel` modal pattern exactly per the styling constraint.

  Live testing (after discovering the whole local stack — Docker Desktop, both dev servers — had silently stopped between turns, likely from the same process interruption that briefly dropped the ExitPlanMode call; restarted everything, including a `.next` cache clear to rule out stale HMR state as a cause of a later red herring) found and fixed three real bugs, none of them hypothetical: (1) **the recovery-key reveal never actually rendered, even before this session** — `handleRegister` calls `setRecoveryKey(key)` then `setSession(newSession)` back to back; React batches both into one render, and since `session` is already truthy in that render, the pre-auth screen showing the key (which lives inside the `if (!session)` branch) was never taken at all — the key display was dead code from the moment registration succeeded, a latent bug that simply had no consequence until this pass needed the key to actually be visible. Fixed with a dismissible banner in the authenticated view too, not just pre-auth. (2) **Logging back in raced against the existing contacts-upload effect** (`useEffect` watching `[contacts, session]`, uploads to the server backup whenever either changes) — the instant `session` becomes non-null post-login, that effect fires using whatever's already in `contacts` (empty, left over from the earlier logout), silently overwriting the real server backup with `[]` before the login flow's own contacts-fetch ever got a chance to run. Confirmed via direct DB query (`encrypted_backups` ciphertext length dropped to 18 bytes — an empty array). Fixed by moving the fetch-and-restore-contacts step to *before* `setSession`, not after, in both `handleLogin` and `handleLinkDevice` — this ordering means the upload effect either sees the real data already or harmlessly re-uploads it, instead of racing to clobber it. Lost the live test's in-progress "buddy" contact to this exact race before the fix landed; re-did the test cleanly with a fresh contact afterward. (3) **`requestPairingCode()` was the first-ever bodyless POST call through `apiFetch`** — every prior call always passed a real body; `apiFetch` unconditionally sent `Content-Type: application/json` regardless, and Fastify's default body parser rejects that exact combination (`FST_ERR_CTP_EMPTY_JSON_BODY`) outright. Diagnosed by checking Next.js's `__nextjs_original-stack-frames` requests (the same technique used earlier this session for the CORS bug) to get the real stack trace, then directly replicating the exact fetch from the browser console to isolate the missing-body-with-json-content-type combination as the actual cause. Fixed at the root in `apiFetch` itself — omit the header when there's no body — rather than patching around it at the one call site, since any future bodyless call would have hit the identical wall.

  Also worth recording as a process note: spent real time chasing a phantom Next.js dev-overlay error (`nextjs-portal` DOM node present, stale "Unexpected eof" text in the console log) that turned out to be inert — the harness's own console-log accumulation (already known from earlier this session) made a resolved error look current. Confirmed it was stale by checking the *server's* actual response via a direct `curl` rather than trusting the browser-side signal, which is now the go-to check when a Next.js dev error seems inconsistent with what the code actually says.

  Verified end to end, three full scenarios: same-device login (log out, log back in with username + recovery key, confirm the contact *and* its verified badge both survive — proving it's genuinely the same crypto identity, not a fresh one); wrong-browser detection (since this browser tool's separate "tabs" all share one origin's IndexedDB — not a real cross-browser test on their own — simulated a genuinely different browser by directly deleting the specific `cycove-<deviceId>` IndexedDB database via `indexedDB.deleteDatabase()`, then confirmed login correctly refused with the "link this device instead" message rather than silently succeeding broken); device linking (both without a recovery key — new device works for messaging, starts with zero contacts, correctly shown as unverified by existing contacts — and with one — contacts restore too). All three passed. `docs/api/openapi.yaml` (bumped to 0.4.0-draft), `docs/ux-flows.md`, `THREAT_MODEL.md`, and `Projects/CyCove.md` all updated with what shipped and its disclosures (linked devices inherit no trust; a stolen recovery key alone doesn't grant crypto impersonation, only account-adjacent capabilities like linking a new device).
- 2026-07-20, continued: Ben asked for phone-browser access via QR-code pairing, and explicitly asked whether this deviates from the original "ultra-safe" goal. Split the ask before building anything: "access from a phone" is likely already free (Tailscale, if the phone joins the tailnet), the real feature is a QR-code way to run the device-linking flow shipped earlier the same day. Answered the security question directly rather than letting it be implied by the plan: the pairing token itself doesn't weaken (same single-use, 5-minute-TTL secret), but a QR code changes the *real-world* threat model — a copy-pasted string needs to be read and retyped, naturally hard to intercept by accident or from a distance, while a QR code can be captured by a camera from across a room in an instant, within that same window. Recommended building a safety net that didn't exist at all — a linked-devices list with revoke — alongside QR, since without it QR would be a real, not just cosmetic, step down from "as secure as possible." Ben chose to build both together (asked via AskUserQuestion rather than assumed). Planned via `EnterPlanMode` given the scope: two new dependencies, camera permissions, two new backend endpoints, a schema change.

  Built: `qrcode` (generation) + `jsqr` (decoding, chosen over the native `BarcodeDetector` API specifically because Safari/iOS doesn't support it, and "a phone via a browser" has to mean that too) — both zero-network, small, matching this project's existing bias toward minimal dependencies. `LinkDevicePanel.tsx` renders the pairing code as a QR image alongside the existing text (both stay available, same pattern as WhatsApp Web's QR-plus-manual-entry). New `QrScanner.tsx` — camera access, a video/canvas polling loop, `jsQR` decode, careful stream cleanup on unmount so leaving the screen doesn't leave the camera running. On the backend: `Device.createdAt` (new field, migration via the same manual-SQL-plus-`migrate deploy` workaround as the username/backup migration); a Redis reverse index (`device-sessions:<deviceId>` → set of tokens) added to `issueSessionToken` so a device's live sessions can all be found and killed at once — without it, "revoke this device" would need a full scan over every session key, which doesn't scale and isn't how Redis is meant to be used; new `revokeDeviceSessions()`; `GET /devices` (list the caller's own account's devices) and `DELETE /devices/:deviceId` (revoke — with the critical check that the target device actually belongs to the caller's own account, otherwise any authenticated user could revoke any device by guessing IDs) in `backend/src/routes/devices.ts`; new `DevicesPanel.tsx` ("Your devices," per-device revoke with a lightweight inline confirm step, "(this device)" tag, revoking your own current device allowed not blocked — same as logging out, just remote).

  Live testing found and fixed two more real bugs. (1) **The exact same CORS gap as the PUT-method bug from earlier this session, this time for DELETE** — `@fastify/cors`'s `methods` list still only had `GET, HEAD, POST, PUT` from when that was fixed; adding `DELETE /devices/:deviceId` without also adding DELETE to the CORS config meant the browser silently blocked the actual request (`TypeError: Failed to fetch`, no response at all — worse than an HTTP error, since nothing reaches the server to show up in its logs). Now the second time this exact class of bug has shipped; noted directly in the code comment as something to treat as a standing checklist item for any future non-GET/POST route, not just fixed silently. (2) **A stale token pointed at a since-deleted device crashed with a raw 500 instead of a clean 401.** Traced via the `__nextjs_original-stack-frames` technique (now a repeatedly-useful diagnostic this session) to `prisma.device.findUniqueOrThrow` in the new `GET /devices` route throwing `P2025` when the caller's own device row didn't exist. Root cause: the specific token being tested was issued in the *previous* conversation, before the Redis reverse-index fix existed in this one — so `revokeDeviceSessions` correctly found nothing to clean up for it (the index was never backfilled for pre-existing tokens, a one-time migration gap, not an ongoing flaw — every token issued after the reverse index shipped is covered by construction). But that gap exposed a separate, genuinely new architectural hole worth fixing regardless: nothing before this feature ever deleted a `Device` row, so a valid Redis token could never previously point at a nonexistent device — now that revocation exists, it can, and `findUniqueOrThrow` isn't the right tool for a case that's an expected outcome, not a bug. Fixed by switching `GET /devices` and `DELETE /devices/:deviceId` to `findUnique` + an explicit null check returning a clean 401, matching `requireAuth`'s own error shape — deliberately scoped to just these two new routes rather than auditing every existing `findUniqueOrThrow` call site, since this specific failure mode (device deleted out from under a live token) literally could not happen before device deletion existed.

  Verified: QR round-trip correctness via a standalone script (not a real camera — this environment doesn't have one) using the *exact* `qrcode`/`jsqr` libraries the app uses — generated a QR from a real pairing code, built the raw pixel bitmap directly from the QR module matrix (skipping PNG decoding entirely, which is what a camera frame reduces to anyway once `jsQR` gets it), decoded it, confirmed an exact match; devices list + revoke live end to end, including catching and fixing both bugs above along the way; cross-account authorization — attempted to revoke a different account's device, correctly rejected with 404, confirmed the target device was untouched afterward. Not testable here: an actual phone-in-hand camera scan — flagged clearly for Ben to confirm once this ships, since everything up to that boundary (generation, decoding correctness, the linking path a successful scan feeds into) is proven. `docs/api/openapi.yaml` (bumped to 0.5.0-draft), `docs/ux-flows.md`, and `THREAT_MODEL.md` (new "Unauthorized device linking via QR capture" section — the direct answer to Ben's "still ultra-safe?" question, written where it'll actually be found later, including the one real residual gap: nothing notifies the account owner when a new device links, discovery still requires opening "Your devices" and noticing something unfamiliar) all updated.
- 2026-07-20, continued: Ben asked what to build next after confirming QR linking worked well. Verbally recommended "real encrypted key backup + cross-signing" — wrong framing, caught before writing any code: `matrix-sdk-crypto-wasm`'s key-backup feature is Megolm *room*-key backup, and CyCove has no rooms/group messaging. Cross-signing itself is real, separate, and library-supported, and directly solves two gaps already disclosed and deferred in the two entries above: linked devices inheriting no trust, and SAS verification only ever covering the one device compared. Planned via `EnterPlanMode`.

  Grounded before designing anything (per this session's established discipline) — a throwaway Node script (`fake-indexeddb`, two Alice devices + one Bob device, no product code) answered every open question concretely: (1) `uploadSigningKeysRequest`/`uploadSignaturesRequest`'s exact body shapes; (2) `/keys/signatures/upload` cannot be a pure opaque pass-through like every other endpoint — a naive overwrite of stored signatures drops a device's own pre-existing self-signature and silently corrupts the record (reproduced the corruption directly: the device vanished from a peer's key-query results), so the endpoint merges (unions `signatures[user_id]`) instead; (3) `bootstrapCrossSigning()` alone does *not* mark your own device `isCrossSignedByOwner()` — an explicit follow-up `OwnUserIdentity.verify()` call is required, a 4th bootstrap step beyond the library's own 3; (4) a newly linked device does not inherit trust automatically — an already-cross-signed sibling has to explicitly sign it (`Device.verify()`); (5) `isCrossSignedByOwner()` is a public fact anyone can observe, `isVerified()`/`isCrossSigningTrusted()` are the viewer's own decision, confirmed independent; (6) `sas.confirm()` never auto-cross-signs the peer's identity even with cross-signing bootstrapped on both sides — confirming the planned extra step (`OtherUserIdentity.verify()` after SAS confirms) is genuinely required. Results written into `docs/crypto-integration-notes.md`'s cross-signing section before any backend/frontend code.

  Built: `User.masterKey`/`selfSigningKey`/`userSigningKey` (opaque, served via `/keys/query`); `EncryptedBackup`'s primary key changed from `userId` alone to a composite `[userId, dataType]` — the field's own doc comment already said `dataType` was meant to support more than one backup kind, but the schema never actually allowed it until cross-signing became the second real user of it (migration applied, all 20 pre-existing `contacts` rows confirmed intact afterward). Two new backend routes: `/keys/signing-keys/upload` (opaque) and `/keys/signatures/upload` (the one merge-based endpoint, per the grounding above). `web/src/lib/crypto.ts`: `bootstrapCrossSigningIfNeeded()` (the 4-step sequence), `restoreCrossSigningKeys()`/`backUpCrossSigningKeys()` (reusing the contacts-backup mechanism, generalized to take a `dataType`), `reconcileOwnDevices()` (signs any sibling device lacking a signature), and `confirmSas()` extended to cross-sign the peer's whole identity once SAS confirms. `ensureSessionWith()` also fixed to force a fresh device-list query on every call, not just for newly-tracked users — CyCove has no server-side "device list changed" push at all (a gap already flagged and deliberately deferred in `docs/crypto-integration-notes.md` back on 2026-07-19), and cross-signing is the first feature where that gap actually bites (a contact's newly linked device would otherwise stay invisible indefinitely).

  Live-tested end to end and found one more real gap along the way, fixed live rather than deferred: linked a second device for an already-SAS-verified contact and it did *not* automatically become trusted, because nothing had told the first device a sibling existed to go sign — `reconcileOwnDevices()` only fires when *something* calls it, and linking alone doesn't. Fixed by polling for a new device while the "Link a device" panel is open (signing it the moment it appears) and also reconciling whenever "Your devices" is opened — confirmed via direct DB inspection that a linked device's `deviceKeys.signatures` gained the missing cross-signing entry only after one of those fired, and that a subsequent contact from a different account then saw it as trusted with no new SAS. Full chain verified live: register two accounts, SAS-verify them (confirmed both sides' master keys gained each other's signature), link a second device for one side, trigger reconciliation, confirm the *other* account's client would compute trust for it per the grounding script's already-proven mechanism. `docs/api/openapi.yaml` (bumped to 0.6.0-draft; `/keys/query` response extended; two new endpoints; `/account-data/contacts` generalized to `/account-data/{dataType}`), `THREAT_MODEL.md` (the "linked devices inherit no trust" entry from 2026-07-20 marked resolved, with the residual watching-required gap disclosed plainly), and `docs/ux-flows.md` updated.
- 2026-07-20, continued: live-testing cross-signing, a message only reached one of a verified contact's two devices — cross-signing answers "should I trust this device," not "does my client send to it." Ben's ask, explicit: every device treated the same, and a newly linked device should receive existing chat history, not just messages sent afterward. Split into two mechanisms before designing anything: fan-out (going forward) and history sync (a device that didn't exist yet was never a fan-out target for past messages, no matter how long anything is retained — needs its own mechanism). History sync's storage question was an explicit architectural fork — CyCove's server deletes every message the instant it's delivered, a stated privacy pillar — resolved by Ben: device-to-device transfer (an already-established device pushes its own history to a new sibling, encrypted like any message), not server-side durable storage. Planned via `EnterPlanMode`.

  Grounded first: confirmed `OlmMachine.getMissingSessions()` already covers every known device of a tracked user in one call (no crypto-layer change needed for fan-out — `ensureSessionWith` already establishes sessions with all of a peer's devices), and confirmed no practical size wall for bundling ~200 messages into one history-sync event (~40KB raw JSON, trivial against a 100MB WS `maxPayload` default). Results in `docs/crypto-integration-notes.md`.

  Built: `Contact.deviceIds` (`web/src/lib/store.ts`) as the seed/verification-anchor device, not the live send target — `sendMessage`/`sendTyping`/`sendReadReceipt` (`crypto.ts`) now fetch a contact's current device list fresh at send time (`getContactDevices`, reusing cross-signing's own device-discovery) and fan out one encrypted copy per device; no backend change needed, same `relayToDevice` endpoint called once per device. History sync: a new `m.cycove.history_sync` to-device event sent to your own userId + a sibling deviceId (zero new crypto primitives — reuses `encryptAndSend`/`ensureSessionWith` targeting yourself), triggered from the exact same device-discovery pass cross-signing already built (`reconcileOwnDevices()` now returns newly-signed device IDs; the caller pushes history to each). Conversation history moved from `sessionStorage` to IndexedDB, keyed per-deviceId like the crypto library's own store, since device-to-device transfer needs the source device to still have its history around.

  Live-tested and found two more real bugs, both fixed live. (1) **A genuine message-loss race**: the first version saved conversations to IndexedDB fire-and-forget from a React effect; reloading a tab in the narrow window before that write completed lost a just-received message — worse, lost the very history this feature exists to sync (reproduced directly: a linked device's IndexedDB conversations store was confirmed empty after a rapid reload, despite the UI having shown the messages). Fixed by making the save complete synchronously at the point of each mutation, awaited before acking the message to the server (`applyConversationsUpdate` in `ClientApp.tsx`) — a reload/crash mid-write now leaves the message in the server's queue (deleted only on ack) for redelivery next reconnect, instead of dropping it. (2) **A stale `web/.env.local` `NEXT_PUBLIC_API_BASE`** still pointing at the Tailscale HTTPS URL from earlier session work — a routine dev-server restart (done to rule out stale build state while chasing bug #1) caused Next.js to actually pick it up for the first time, breaking every backend call and looking exactly like a verification regression until traced via a caught exception (`TypeError: Failed to fetch` in `ensureSessionWith`) to its real cause. Repointed at `http://localhost:3000` for local dev testing, with Ben's explicit go-ahead first since it's his environment config.

  Full chain verified live after both fixes: registered two accounts, SAS-verified (confirmed cross-signing still intact), built up real conversation history, linked a second device — instantly trusted *and* carrying the full prior history — sent a new message from the contact and confirmed it fanned out to both devices simultaneously, linked a third device and confirmed the same, reloaded a linked device and confirmed both its verified status and its IndexedDB-persisted history survived (the actual regression test the env bug had been blocking). `docs/ux-flows.md` and `THREAT_MODEL.md` (new "Multi-device history sync depends on another device being online" section, disclosing the real limitation) updated. No backend schema/route changes were needed — both mechanisms ride entirely on the existing generic to-device infrastructure.
- 2026-07-21: Ben paid Hetzner's one-time 25€ identity verification fee and provisioned the box, well ahead of the original ~August budget target. Walked the entire Hetzner console UI live via screenshots — Type (Arm64 CAX line was out of stock across every EU datacenter checked: Nuremberg, Falkenstein, Helsinki; accepted x86 CX23, 2 vCPU/4GB/40GB, as a fine fallback — it actually removes a concern, no Arm64 Docker compatibility question, rather than adding one), Volumes (skipped, not needed yet), Firewalls, Backups (Ben opted in, +9€/mo), Placement groups/Labels/Cloud config (skipped — a separate VPS was decided for the unrelated MeshNest ticketing service rather than co-hosting, to keep blast radius separated), Name, Image (Debian 13 trixie), Networking (public IPv4+IPv6, no private networking needed for a single-box setup). Server created at `168.119.229.29`.

  SSH access and hardening, done live over direct SSH: initial root login, created a non-root sudo user (`ben`, `NOPASSWD:ALL` via `/etc/sudoers.d/ben`), then locked down `/etc/ssh/sshd_config` (`PermitRootLogin no`, `PasswordAuthentication no`) — verified the new user's key-based access worked *before* disabling root/password login, not after, to avoid a self-lockout. Hetzner Cloud Firewall configured: SSH (TCP 22) restricted to Ben's own static IP as a `/32` (his public IP is dynamic day-to-day but was static enough for this session; DDNS was raised as a future option — Hetzner firewalls only match resolved IPs/CIDRs, no hostname support, so a real DDNS-tracking solution would need a small script hitting the Hetzner API on IP change, deferred as unnecessary for now), ICMP open to any. Docker CE + Compose plugin installed via Docker's official apt repo — confirmed it already has native Debian 13 (trixie) support, no bookworm-compatibility fallback needed. Verified with `docker run hello-world` as the non-root `ben` user (docker group membership).

  Discovered the actual CyCove repo had **zero git history and no remote** — everything built across this entire project existed only as uncommitted working-tree files on Ben's machine, no off-machine backup at all. Flagged this plainly rather than just doing a one-off file copy to the server, given how much was already built. Ben created a private GitHub repo (`github.com/BenBodrogi/cycove`); local repo initialized, first commit made (66 files, the entire Phase 0–1 history in one snapshot, `.gitignore` already correctly excluding `.env`/`.env.local`/`node_modules` — double-checked no secrets were staged before committing), pushed over SSH once Ben added the local machine's existing key to his GitHub account. Server-side access used a **separate, repo-scoped read-only deploy key** (generated on the server itself, added as a GitHub deploy key rather than reusing Ben's personal key or granting the server broader account access) — repo cloned to `~/cycove` (had to install `git` on the box first, Docker alone wasn't enough).

  Wrote `docker-compose.prod.yml`: Postgres/Redis never published to the host at all (only reachable over the compose network), backend bound to `127.0.0.1` only pre-reverse-proxy. Production secrets (`backend/.env` — DB password generated with `openssl rand -hex 24`, `DATABASE_URL`, `REDIS_URL`) written directly on the server via SSH, never through git. First deploy attempt found and fixed two real bugs in `backend/Dockerfile`, neither previously exercised since it had never actually been built before: (1) `postinstall` runs `prisma generate`, but the multi-stage build only copied `package*.json` before `npm ci` — `schema.prisma` wasn't in the build context yet, breaking the install outright; fixed by copying `backend/prisma` before `npm ci`. (2) The runtime stage copied `/app/backend/node_modules`, which doesn't exist — npm workspaces hoists dependencies to `/app/node_modules` at the repo root; fixed the copy path. Once building cleanly, ran `prisma migrate deploy` inside the backend container — all 6 existing migrations applied cleanly against the fresh production database.

  Added a Caddy reverse proxy (`Caddyfile`, new `caddy` service in `docker-compose.prod.yml`) for automatic Let's Encrypt TLS on `cycove.virdana.dev` (a Virdana subdomain, Ben's choice — DNS lives at Spaceship, Ben added the `A` record pointing at the server's IP; Hetzner firewall opened for TCP 80/443 to any, unlike the IP-restricted SSH rule, since public web traffic has to be reachable from anywhere). This meant the backend's `trustProxy` setting — previously hardcoded to `'loopback'` for the old Tailscale-Serve-on-127.0.0.1 dev setup — needed to change, since Caddy now proxies over the internal Docker network, not loopback; made it configurable via `TRUST_PROXY`, pinned the compose network to an explicit `172.28.0.0/16` subnet so production can trust exactly that CIDR rather than reaching for a blanket `true`. First deploy with Caddy found one more real bug, this time infra rather than code: recreating the compose network (to add the pinned subnet) left the already-running `redis` container silently disconnected from its `redis` service-name DNS alias — `postgres` and `backend` got recreated fresh and picked up correct aliases automatically, `redis` didn't need rebuilding so compose just reconnected its existing container, which kept it network-reachable by IP but not by name. Diagnosed via `docker inspect`'s `DNSNames` field showing `postgres` had `["cycove-postgres-1","postgres",...]` while `redis` only had `["cycove-redis-1",...]` — no `redis` alias at all. Fixed with a targeted `--force-recreate redis`.

  Verified live end to end: Let's Encrypt cert obtained on first request (confirmed via Caddy's logs — ACME account registered, HTTP-01 challenge solved, certificate obtained); `https://cycove.virdana.dev/health` returns `200 {"status":"ok"}` with a real trusted cert (no `-k` needed); port 3000 confirmed unreachable directly from the internet (connection timeout, not just a firewall reject — Docker itself only binds it to loopback); backend request logs confirm `remoteAddress` shows the real caller's IP through Caddy, not Caddy's own container IP, proving the `TRUST_PROXY` fix actually works and rate-limiting will key on real users rather than collapsing everyone into one bucket. Also noted, not a concern: the server started receiving generic internet-wide scanner traffic (`/​.env`, `/​.DS_Store`, cPanel/registry-catalog probes) within minutes of ports 80/443 opening — universal background noise for any newly-exposed public IP, all correctly 404'd, not a targeted or successful attack.
- 2026-07-21, continued: Ben visited `cycove.virdana.dev` and got a bare `{"message":"Route GET:/ not found"...}` — the deploy above only ever stood up the backend API; the actual Next.js web client had never been containerized or put on the server at all, still only ever run via `npm run dev` locally. Split the two onto separate subdomains rather than path-routing one origin (`api.cycove.virdana.dev` for the backend, `cycove.virdana.dev` for the web app) — matches how the client already treats `NEXT_PUBLIC_API_BASE` as a full separate origin, same pattern the earlier Tailscale Serve dev setup used. New `web/Dockerfile` (Next.js `output: 'standalone'`, added to `next.config.mjs`; `NEXT_PUBLIC_API_BASE` passed as a build ARG since Next.js inlines `NEXT_PUBLIC_*` vars into the client bundle at build time, not read at runtime) and a `web` service in `docker-compose.prod.yml`. Ben added the `api.cycove.virdana.dev` DNS record at Spaceship the same way as before.

  Also tightened `backend/src/index.ts`'s CORS config from `origin: true` (wide open) to a `CORS_ORIGINS` env-driven allowlist — the code's own existing comment already flagged this as "revisit before anything is reachable outside the LAN," and that condition just became true. First deploy attempt hit a transient `npm ci` network error (`ECONNRESET` against the registry, not a code issue — retried clean). Caddy needed an explicit restart to pick up the new two-site `Caddyfile`, since it only reads config at startup, not on a bind-mounted file change; confirmed the earlier `redis` DNS-alias fix held (checked `docker inspect`'s `DNSNames` again) rather than assuming it survived the network-affecting redeploy. Verified live: `cycove.virdana.dev/` now serves the real Next.js app (200, correct HTML), `api.cycove.virdana.dev/health` returns `200 {"status":"ok"}` on its own cert, and `cycove.virdana.dev/health` correctly 404s (confirming the two origins are genuinely separate, not accidentally still sharing one backend). Noted for Ben, not fixed without asking: the page's `<title>`/description in `web/app/layout.tsx` are still the original scaffolding placeholder ("CyCove — dev test client... not the real product UI") from 2026-07-19, never updated as the real chat UI got built out over the following two days — a copy/content decision, not a functional bug.
- 2026-07-21, continued: Ben asked what's next on the roadmap; after a rundown of remaining gaps and a brainstorm of further ideas, said to take the wheel on implementation. Picked the batch of low-risk, high-value features that reuse the proven to-device event pattern and need zero backend changes — reactions, reply/quote, delete for me/everyone, forwarding, a read-receipt opt-out toggle — over the two large, separately-scoped items (media attachments, group messaging) that need their own design pass first. Planned via `EnterPlanMode`. While designing delete-for-everyone's own-device consistency, found a real, already-shipped gap worth folding into the same pass: a message sent from one already-linked device never live-echoed to your other already-linked devices, only a *newly* linked one via the one-time bulk history-sync — added as item 6.

  Built a new shared `fanOutConversationEvent` (`crypto.ts`) — sends a per-message event to every current device of the contact and every other device of the caller's own account, reusing `encryptAndSendToAllDevices` plus a direct loop over `listDevices()`; own-device copies carry an explicit `contactUserId`, same reason `sendHistorySync`'s payload does. `sendMessage` itself now goes through this (closing the own-device echo gap for free), alongside new `sendReaction`/`deleteMessageForEveryone`. New wire content: `m.cycove.message` gained an optional `replyToId`; new `m.cycove.reaction` and `m.cycove.delete` event types. `ChatMessage` (`store.ts`) gained `replyToId`/`reactions`/`deleted`. New `ForwardMessagePanel.tsx` (same modal convention as the existing contact/device panels); `Conversation.tsx` gained a per-bubble action row (Reply/React/Forward/Delete), a fixed 6-emoji reaction picker, a reply-strip above the composer, and a deleted-message placeholder; `Sidebar.tsx` gained a read-receipts on/off toggle.

  Before any of this could be tested, Ben reported registration completely broken in production: stuck forever on "Registering…" with `Uncaught Error: Expected a KeysUploadRequest (with an id) immediately after OlmMachine.initialize()`. Reproduced live directly against `cycove.virdana.dev` (not locally — this only happens in the production build) via the Browser tool, then root-caused it concretely rather than guessing: monkeypatched `Array.prototype.find` in the live deployed app to capture what `outgoingRequests()` actually returned, and found the real request objects were present and correctly shaped, just with `constructor.name` values of `"X"` and `"J"` instead of `"KeysUploadRequest"`/`"KeysQueryRequest"`. Root cause: every request/event-type check throughout `crypto.ts` (and one in `ClientApp.tsx`) used `.constructor.name === 'SomeClassName'` string comparisons — these worked in every local dev test throughout this entire project (Next's dev server doesn't minify) and silently broke the instant the app was actually built for production, since Next's minifier renames classes for size. Not limited to registration — the identical pattern gated `login()`/`linkDevice()`'s keys-not-on-this-device check, `relayOutgoing`, the whole `processOutgoingRequests` dispatch switch, `handleIncoming`'s decrypted-event check, and `ClientApp.tsx`'s Sas-vs-Qr check, meaning login, linking, messaging, and verification were all equally broken, not just the one flow that happened to be tried first. Fixed by importing the real classes (`KeysUploadRequest`, `KeysQueryRequest`, `KeysClaimRequest`, `ToDeviceRequest`, `DecryptedToDeviceEvent`, `Sas`) and using `instanceof` everywhere instead, which survives minification since it checks the actual (consistently renamed) class binding rather than a stale string literal. Full writeup, including the general lesson for this codebase going forward, in `docs/crypto-integration-notes.md`.

  Typechecked and built cleanly locally before deploying (both fixes landed in the same commit, since they ended up touching the same functions). Deployed, then re-ran the exact live reproduction against production again — clean registration this time, no errors, full session established. Ben separately asked to clear all test accounts from the production database to start fresh — truncated `users`/`devices`/`encrypted_backups`/`one_time_keys`/`to_device_queue` (left `_prisma_migrations` alone) and flushed Redis (stale sessions/rate-limit counters tied to the deleted accounts), confirmed all tables empty afterward.

  Full live verification of the quick-wins bundle itself followed, using fresh test accounts (cleared again afterward): registered two accounts, connected and SAS-verified them, sent a message, reacted to it from both sides (confirmed the pill updates on both), replied (confirmed the quoted snippet renders on both sides), deleted a message for everyone (confirmed it tombstoned on the recipient's own tab, not just the sender's), forwarded a message, and toggled read receipts off (confirmed the sender's status stayed at "delivered" instead of advancing to "read"). Multi-device: linked a second device for the same account with the recovery key supplied — confirmed it inherited trust and full history automatically (same mechanism as the earlier multi-device phase) — then sent a new message from the first device and confirmed it appeared live on the second without any reconcile/relink, the direct regression test for the own-device-echo fix. `docs/crypto-integration-notes.md` (two new sections: the production bug, and the quick-wins design), `docs/ux-flows.md`, and `THREAT_MODEL.md` (new "delete for everyone is a courtesy, not a guarantee" section) all updated.
- 2026-07-21, continued: Ben reported the Log out button was unreachable, and asked for an "OPEN ALPHA" label on the front page and tab title. Root cause of the button: `Sidebar.tsx`'s header row packed 5 buttons (Add/Link device/Your devices/Read receipts/Log out) into one unwrapped flex row inside a fixed 280px-wide sidebar — they overflowed the container, pushing the last one (Log out) off-screen. Fixed with `flexWrap: 'wrap'` so buttons always stay reachable regardless of count; verified live by checking `document.elementFromPoint()` at the button's actual screen position matched the button itself (nothing overlapping it) before and after, then clicking it for real and confirming a clean logout. Labeled OPEN ALPHA in `layout.tsx`'s `<title>` (replacing the stale "dev test client" placeholder flagged but not fixed earlier the same day) and as a small badge next to the pre-auth `<h1>`.
- 2026-07-21, continued: Ben said he liked all 5 quick-win ideas from the earlier brainstorm, plus flagged the per-message action row from the reactions/reply/delete/forward pass as getting crowded and suggested a Messenger-style 3-dot menu — explicitly invited questions if anything was unclear, but none of the five turned out to need one (all resolved with precedented defaults: notification permission only ever requested in direct response to the toggle, never on load; notification content shows the contact label but never the message body, matching but not identical to FCM's fully-opaque design, reasoned through and written up in `THREAT_MODEL.md`).

  Shipped: the crowded action row collapsed into a "⋯" dropdown (Reply/React/Forward/Delete for me/Delete for everyone), plus a new **Copy** action — backdrop-click-to-close, same pattern the modal panels already use, not a racy document listener; message bubbles gained a **timestamp** alongside the existing status ticks; `Sidebar.tsx` gained a **contact search** input (pure client-side filter); a new **browser notifications** toggle (off by default, fires only when the tab is backgrounded, shows sender label never body); and **Escape-to-close** on all 5 modal panels via a new shared `useEscapeKey` hook. Two new refs (`contactsRef`, `notificationsEnabledRef`) were needed for the notification firing logic specifically — `handleIncomingEvent` is only re-captured by the WS connection effect when `session` changes, so a plain `contacts`/`notificationsEnabled` read inside it would freeze at whatever value existed when the connection was first set up (the same staleness class `conversationsRef`/`sessionRef` already exist to avoid, just newly relevant here since this is the first feature reading contacts/settings state from inside that specific closure).

  Typechecked and built cleanly locally (double-checked `grep`'d for any new `.constructor.name` patterns given the bug found earlier the same day — none). Deployed, then live-verified every piece end to end with fresh test accounts (cleared again afterward): confirmed the "⋯" menu opens with all 6 items and closes on backdrop click; confirmed the timestamp renders alongside status; confirmed Escape actually closes an open modal panel (`document.elementFromPoint` / DOM-presence checks, not just "the click handler exists"); confirmed the search input filters to "no contacts match" and back; confirmed the notifications toggle flips state and calls `requestPermission()` without throwing (the automated test browser auto-denies the actual OS prompt, a sandbox limitation, not a code issue — the gating logic itself, `Notification.permission !== 'granted'`, was confirmed correct). `docs/ux-flows.md` (new section) and `THREAT_MODEL.md` (new "Browser Notification API popups" entry) updated.
